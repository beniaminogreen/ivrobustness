% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rv.R
\name{interpret_table}
\alias{interpret_table}
\title{Interpret a Table Of Robustness Values}
\usage{
interpret_table(df)
}
\arguments{
\item{df}{a datasdrame, as returned by \code{robustness_values}}
}
\value{
A string
}
\description{
Struggling to interpret the results of \code{robustness_values}? Look no further!
\code{interpret_table} helps by providing a plain-text interpretation of the robustness
values of a design. It interprets each of the four robustness values for you,
and flags low robustness values.
}
\examples{
create_simulation <- function(n=10^7, rho = .5) {
 w <- rnorm(n)
 z <- sqrt(rho)*w + sqrt(1-rho)*rnorm(n)
 y <- 2*z + rnorm(n)

 return(list(
   w = w, 
   z = z, 
   y = y
 ))
}


weak_instrument_data <- create_simulation(rho = .01)

df <- robustness_values(
 weak_instrument_data$w, 
 weak_instrument_data$z, 
 weak_instrument_data$y
)
interpret_table(df)
}
